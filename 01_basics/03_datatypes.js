"use strict"; //to save old code from new JS features, and standard practise, treats as newer version of js
//alert(3+3) //wont work as not using browser, using nodejs
/* Primitive
number (2^53), bigint, string, bool,
null (standalone value, not 0, not undefined), undefined (declared but not assigned a value)
symbol(used mostly in react to find uniqueness of components, also in figma) 
 */

//Object
console.log(typeof 3); //number
console.log(typeof "ajay"); //string
console.log(typeof null); //output is object
console.log(typeof undefined); //output is undefined